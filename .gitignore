# ============================================================
# .gitignore — Consentimiento de Tatuajes (GitHub Pages)
# Los archivos de esta lista NO se suben al repositorio.
# ============================================================

# ---- Archivos locales de prueba (no forman parte de la web) ----
iniciar.bat
servidor.ps1

# ---- Backup local del proyecto (NUNCA subir a GitHub) ----
# La carpeta 'backup' contiene copias del proyecto y el context del
# chat. Puede incluir datos privados; mantenerla fuera del repo.
backup/
context.txt

# ---- PDFs de ejemplo (el PDF real se genera desde el código) ----
*.pdf

# ---- Sistema / SO ----
Thumbs.db
.DS_Store